#include <stdio.h>

int main() {
    int a;
    printf("enter:");
    scanf("%d",&a);
    if (a>=10)
    {
        printf("every thing is fine");
    }
    else
    {
        printf("number is less than 10");
    }
}
